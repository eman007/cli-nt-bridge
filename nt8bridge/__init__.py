# Keep in step with pyproject.toml's `version`. `selfcheck` compares the two AND the installed dist
# metadata — all three disagreed (0.1.0 / 1.6.0 / 1.2.0) the first time that check was ever run.
__version__ = "1.7.0"
